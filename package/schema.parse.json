{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsRouterKitParse",
  "title": "@routerkit/core parse schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project"
    },
    "dryRun": {
      "type": "boolean",
      "description": "Parse project without touching filesystem"
    }
  },
  "required": ["project"]
}
